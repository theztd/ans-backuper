---
backup_db_script: true
backuper_db_dir: /home/backups/db
backup_fs_script: true

s3_sync:
- name: Databases
  backup_dir: /data/backups
  bucket_path: "s3://test2delete/{{ ansible_fqdn }}"

#s3_access_key: CHANGE_ME
#s3_secret_key: CHANGE_ME
#s3_endpoint: CHANGE_ME
# Secured transfer
#s3_gpg_passphrase: ENCRYPTED_GPG_KEY
s3_host_bucket: "%(bucket)s.{{ s3_endpoint }}"
s3_bucket_location: US
